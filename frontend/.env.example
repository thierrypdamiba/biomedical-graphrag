# =============================================================================
# Biomedical GraphRAG Frontend - Environment Configuration
# =============================================================================
# Copy this file to .env.local and configure as needed.
#
# QUICK START (uses hosted backend - no credentials needed):
#   cp .env.example .env.local
#   pnpm install && pnpm dev
#
# The hosted backend at AWS provides full GraphRAG functionality.
# =============================================================================

# -----------------------------------------------------------------------------
# GraphRAG API Backend (Required)
# -----------------------------------------------------------------------------
# Hosted backend (recommended for quick start - already has data):
GRAPHRAG_API_URL=https://ihdx3ugyrv.us-east-1.awsapprunner.com

# For local development, run the backend from biomedical-graphrag repo:
# GRAPHRAG_API_URL=http://localhost:8765

# -----------------------------------------------------------------------------
# Qdrant Configuration (Optional - only for direct Qdrant access/fallback)
# -----------------------------------------------------------------------------
# Only needed if you want to bypass the GraphRAG backend or use fallback mode.
# The hosted backend already connects to Qdrant, so these are optional.

# QDRANT_URL=https://your-cluster.qdrant.io
# QDRANT_API_KEY=<your-qdrant-api-key>
# QDRANT_COLLECTION_NAME=biomedical_papers

# -----------------------------------------------------------------------------
# OpenAI Configuration (Optional - only for direct embedding generation)
# -----------------------------------------------------------------------------
# Only needed if using direct Qdrant search without the GraphRAG backend.
# The hosted backend handles embeddings, so this is optional.

# OPENAI_API_KEY=<your-openai-api-key>
